import * as assert from 'assert';
import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { List } from './';

describe('List', () => {
  it('renders without crashing', () => {
    // ReactDOM.render(<List title='Noice' />, null);
  });
});
